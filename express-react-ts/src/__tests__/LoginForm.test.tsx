import { render, fireEvent, waitForElement } from "@testing-library/react";

import { LoginPage } from "../client/comps/Pages/LoginPage";
import React from "react";

describe("<LoginPage />", () => {
  test("should display a blank login form", async () => {});
});
